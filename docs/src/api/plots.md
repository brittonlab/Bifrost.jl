```@meta
CollapsedDocStrings = true
```

# [Plots](@id plots)

Opt-in visualization helpers for paths, fibers, and propagation diagnostics. These are
loaded separately with `using Bifrost.Plots` so that a plain `using Bifrost` does not pull
plotting symbols into scope.

```@autodocs
Modules = [Bifrost.Plots]
Filter = is_public_api
```
